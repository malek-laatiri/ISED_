<form method="post" action="{{ path('uv_delete', {'id': uv.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uv.id) }}">
    <button class="btn">Delete</button>
</form>
