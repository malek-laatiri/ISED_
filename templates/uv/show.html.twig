{% extends 'base.html.twig' %}

{% block title %}Uv{% endblock %}

{% block body %}
    <h1>Uv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uv.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ uv.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('uv_index') }}">back to list</a>

    <a href="{{ path('uv_edit', {'id': uv.id}) }}">edit</a>

    {{ include('uv/_delete_form.html.twig') }}
{% endblock %}
